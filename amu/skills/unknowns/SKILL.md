# Handling unknown nodes

## When to use
unknowns

## Steps
- unknown never becomes sound by reading source; use `amu verify --node nX --attempt-fallback` or ask a human

## Done when
- the command in the last step printed its `next` line and no blocking violation remains
