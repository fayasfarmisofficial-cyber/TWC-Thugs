# Map before you touch anything

## When to use
map-a-change

## Steps
- run `amu map --symbol <path#name> --change <class>`; read radius/resolution; every guessed/unknown node has a verify line

## Done when
- the command in the last step printed its `next` line and no blocking violation remains
