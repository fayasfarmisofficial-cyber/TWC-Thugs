# Finish a task

## When to use
close-the-loop

## Steps
- `amu done` = sync → sweep → docs → memory refresh → report; done never exits 1

## Done when
- the command in the last step printed its `next` line and no blocking violation remains
